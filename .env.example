# MediaLive Distributor Environment Configuration

# Configuration directory for persistent storage
CONFIG_DIR=/app/config

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# API host and port (usually no need to change)
API_HOST=0.0.0.0
API_PORT=8000

# Maximum number of concurrent channels (optional, for documentation)
MAX_CHANNELS=25

# RTP receive buffer size (optional, system tuning)
# RTP_BUFFER_SIZE=134217728

# WebSocket ping interval in seconds
# WS_PING_INTERVAL=30
